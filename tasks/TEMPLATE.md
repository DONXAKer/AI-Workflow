---
id: FEAT-AREA-NNN
title: Краткое название задачи
created: YYYY-MM-DD
status: planned     # planned | in_progress | done | blocked
# done_date: YYYY-MM-DD   # заполняется при переводе в done/
---

## Как сейчас

<!--
Что есть в текущем коде/системе. Ссылки на конкретные файлы и классы.
Почему это стало проблемой, неполно, или требует изменения.
Раздел обязателен — без него Клод не понимает отправной точки.
-->

## Как надо

<!--
Какое наблюдаемое поведение должно быть после выполнения задачи.
Конкретно, не "улучшить UX", а "при выборе юнита подсвечивается ячейка и
показывается попап со статами в правом верхнем углу".
Раздел обязателен.
-->

## Вне scope

<!--
Что НЕ трогать в этой задаче.
Явно перечислить соседние системы, которые могут показаться релевантными,
но решаются отдельно.
Без этого раздела Клод начнёт "заодно" рефакторить всё вокруг.
Раздел обязателен.
-->

## Критерии приёмки

<!--
Observable-факты, проверяемые тестом, валидатором, gradle build'ом, или прямой
проверкой в UI.
В active/ — `- [ ]`. При переводе в done/ — `- [x]`.

Пример хороших:
  - [ ] HttpGetBlockTest зелёный
  - [ ] Блок зарегистрирован в PipelineRunner (виден в boot logs)
  - [ ] `gradle build` зелёный
  - [ ] Playwright e2e `tests/ui/active-runs.spec.ts` проходит

Пример плохих (не проверяемые):
  - [ ] UX лучше
  - [ ] Код чище
-->

- [ ]
- [ ]
- [ ]

## Реализовано

<!--
Заполняется ТОЛЬКО при переводе задачи в done/. Что именно было создано/изменено:
конкретные файлы, классы, тесты, миграции, UI-страницы.
Используется как индекс артефактов задачи.

Структура:

**Файлы:**
- `путь/к/файлу.kt` (новый | изменён)
- `workflow-ui/src/pages/Foo.tsx` (что изменено)

**Тесты:**
- `workflow-core/.../FooTest.kt` — что покрывает
- `tests/ui/foo.spec.ts` — Playwright сценарий

**Связанные документы:**
- `docs/foo-architecture.md` (если есть подробный how-to)
-->
