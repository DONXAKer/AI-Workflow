---
title: Add goodbye function
---

## Описание

Добавить функцию `goodbye()` в `src/hello.ts`, возвращающую строку `"bye"`.

## Acceptance

- [ ] `src/hello.ts` экспортирует `goodbye(): string`
- [ ] `goodbye()` возвращает строго строку `'bye'`
- [ ] Существующая функция `hello()` остаётся без изменений
